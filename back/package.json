{
  "name": "back",
  "version": "1.0.0",
  "description": "Proyecto de consumo de un back con json-server desde react",
  "main": "database.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "json":"json-server --watch --port 5174 database.json"
  },
  "author": "Jaime Zapata Valencia",
  "license": "ISC",
  "dependencies": {
    "json-server": "^1.0.0-alpha.23"
  }
}
